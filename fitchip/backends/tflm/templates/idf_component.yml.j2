# ESP-IDF Component Manager manifest — pulls TFLite Micro (+ esp-nn kernels
# on Xtensa targets) as a managed component on the first build.
dependencies:
  idf: ">=5.1"
  espressif/esp-tflite-micro: "^1.3.1"
