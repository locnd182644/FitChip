; Generated by FitChip v{{ fitchip_version }}
; Same sources build under ESP-IDF (idf.py) and PlatformIO (this file):
; both use the espidf framework, so main/idf_component.yml applies to both.

[env:{{ target.id }}]
platform = espressif32
board = {{ pio_board }}
framework = espidf
monitor_speed = 115200
build_flags =
    -Os
