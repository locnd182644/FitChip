# Generated by FitChip v{{ fitchip_version }}
CONFIG_ESP_MAIN_TASK_STACK_SIZE=8192
CONFIG_COMPILER_OPTIMIZATION_SIZE=y
{% if target.psram_kb %}
# {{ target.display_name }} has {{ target.psram_kb }} KB PSRAM. Uncomment to enable it
# (then the tensor arena can be moved there with heap_caps_malloc):
# CONFIG_SPIRAM=y
{% endif %}
